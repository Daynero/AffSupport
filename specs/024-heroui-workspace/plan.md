# Implementation Plan: The team workspace on HeroUI

**Branch**: `024-heroui-workspace` | **Date**: 2026-09-16 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `/specs/024-heroui-workspace/spec.md`

## Summary

Re-lay the team workspace on HeroUI v3 (React Aria + Tailwind v4), themed from Soty's existing
token layer, and — while every screen is being touched — fix the flows those screens were
forcing. The load-bearing decisions are in [research.md](./research.md):

- The component inventory keeps its public API and its marker classes; only its insides become
  HeroUI (D5). That makes the migration reviewable screen by screen, keeps the "one role, one
  appearance" test working, and hands the new look to the screens outside the workspace for free.
- Soty's `tokens.css` drives the library, not the reverse (D2), so the design gate keeps its
  premise and the exemption list stays empty.
- The token fence moves to where values can now be written — a new check on utilities in TSX
  (D6) — because Tailwind v4's Vite plugin never writes a stylesheet into the source tree.
- One registry defines what can be done to a material, and every surface renders from it (D9).
  That is what makes the owner's example — catalog a video from inside the task that carries it —
  a small change rather than a large one.
- The task editor gets one save model: autosave (D10).

## Technical Context

**Language/Version**: TypeScript 5.9, `strict: true`, ESM `NodeNext`, target ES2022

**Primary Dependencies**: React 19.2.7, Vite 8.1.5, `@heroui/react` 3.2.5, `@heroui/styles`
3.2.5, `tailwindcss` 4.3.1, `@tailwindcss/vite` 4.3.1, `react-aria` 3.52.1,
`react-aria-components` 1.21.1, `@react-aria/i18n` 3.13.1, `@react-aria/ssr` 3.10.1,
`@react-aria/utils` 3.34.1, `@internationalized/date` 3.12.4, `lucide-react` 1.37 (kept)

**Storage**: none new. No migration, no edge function, no RPC, no shared-contract change.

**Testing**: vitest (central `tests/`, `*.test.ts(x)`, jsdom docblock for DOM), single-fork on
this machine; the gate scripts under `scripts/`; hand-walked visual verification on the beta.

**Target Platform**: browser, Cloudflare Pages; the app also runs inside the packaged desktop
shell, which loads the same bundle.

**Project Type**: web application in an npm-workspaces monorepo; this feature touches
`apps/web` only.

**Performance Goals**: first-load weight measured before and after and stated (D7); no
transition of a layout-forcing property; a 500-row folder stays usable.

**Constraints**: web-only deployable — nothing under `apps/agent`, `packaging`,
`packages/shared/src`, `packages/shared/package.json` or `config/production.env` may change, or
the release gate refuses the deploy. Prettier is non-negotiable (`printWidth: 100`, single
quotes, no trailing comma, `arrowParens: avoid`). This machine runs one heavy process at a time.

**Scale/Scope**: 146 files / 32,060 lines under `apps/web/src/team/`; 13 inventory files / 2,856
lines; 25,929 lines of CSS of which 3,529 are team-dedicated; 128 modules importing the
inventory; 104 raw buttons, 34 raw inputs, 11 raw selects to remove.

## Constitution Check

_GATE: must pass before Phase 0, re-checked after Phase 1._

| Principle                                          | Verdict                              | Note                                                                                                                                                                                                                                                            |
| -------------------------------------------------- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **I. Type-safe contracts**                         | PASS                                 | No new boundary. The action registry is a discriminated union (`ok` / `{ reason }`), not a stringly-typed branch. `any` stays out of `src`; HeroUI ships its own types. `@internationalized/date` values are converted at the UI edge, so no API shape changes. |
| **II. One source of truth for release & protocol** | PASS                                 | No version, manifest, artifact or contract fact is touched. `release.ts` untouched. The feature is web-only by construction, so the web-only deploy gate stays satisfiable.                                                                                     |
| **III. Security & least privilege**                | PASS                                 | No SQL, no RLS, no secrets, no new origin. CSP is regenerated by its script; `style-src` already permits the inline positioning React Aria writes. New third-party code is declared in `THIRD_PARTY_NOTICES.md` and audited by `audit-dependencies.mjs`.        |
| **IV. Child processes & resources**                | N/A                                  | No agent work.                                                                                                                                                                                                                                                  |
| **V. HTTP API & error conventions**                | PASS                                 | Machine codes stay machine codes; the single `team/errors.ts` mapper remains the only place a code becomes a sentence. The action registry carries codes, never sentences.                                                                                      |
| **VI. Frontend composition & state discipline**    | **VIOLATION — amended, not excused** | VI currently mandates "one global stylesheet" and "`className` strings against `styles.css`". This feature replaces the mechanism while keeping the discipline. The amendment is task T001 and lands before any library code. See Complexity Tracking.          |
| **Stack & style constraints**                      | PASS                                 | Exact-pinned deps (no `^`, no `latest`). Tests stay in central `tests/`. Prettier reflows long class strings; where a class list would fight `printWidth: 100`, it is composed in a variant map rather than inline.                                             |
| **Workflow & quality gates**                       | PASS                                 | Every gate keeps running; one gate is added (D6). `npm run verify` is not run on this machine — gates run individually here and the aggregate runs in CI.                                                                                                       |
| **Governance**                                     | PASS                                 | The amendment is a PR edit to the constitution with a Sync Impact Report, approved by the owner as maintainer.                                                                                                                                                  |

**Re-check after Phase 1 design**: unchanged. The design adds no new violation; the single
violation is resolved by amendment before implementation begins.

## Project Structure

### Documentation (this feature)

```text
specs/024-heroui-workspace/
├── plan.md                  # This file
├── spec.md                  # What and why
├── research.md              # D1–D15, the decisions
├── data-model.md            # The action registry, context, detail, theme bridge
├── quickstart.md            # How to prove it works
├── contracts/
│   ├── material-actions.md  # The one definition of what can be done to a material
│   ├── theme-bridge.md      # Soty tokens → HeroUI variables → Tailwind theme
│   └── components.md        # The inventory's API after the swap
├── checklists/
│   └── requirements.md
└── tasks.md                 # Phase 2 output
```

### Source code (repository root)

```text
apps/web/
├── package.json                      # + HeroUI, Tailwind, React Aria peers (exact pins)
├── vite.config.ts                    # + @tailwindcss/vite; manualChunks gains `heroui`
├── index.html                        # unchanged (the pre-paint theme commit stays)
└── src/
    ├── main.tsx                      # + the new entry stylesheet, first in the chain
    ├── styles/
    │   ├── tailwind.css              # NEW: layers, tailwind import, heroui styles,
    │   │                             #      @theme inline bridge, dark custom-variant
    │   ├── tokens.css                # + the HeroUI variable block (the only file with values)
    │   ├── components.css            # shrinks as the inventory stops needing it
    │   ├── team-tasks.css            # deleted by the tasks checkpoint
    │   ├── team-accounts.css         # deleted by the accounts checkpoint
    │   └── base.css                  # kept (our reset; Tailwind preflight is not imported)
    ├── styles.css                    # loses every rule a migrated screen orphans
    ├── components/
    │   ├── ui/                       # same API, HeroUI insides, markers preserved
    │   │   ├── types.ts              # uiClasses() keeps emitting markers; + variant maps
    │   │   ├── Button.tsx Badge.tsx Card.tsx Alert.tsx Field.tsx Choice.tsx
    │   │   ├── Feedback.tsx Overlay.tsx Navigation.tsx Table.tsx patterns.tsx
    │   │   ├── DateField.tsx         # NEW: Calendar, RangeCalendar, DatePicker
    │   │   ├── CommandPalette.tsx    # NEW
    │   │   ├── ContextMenu.tsx       # NEW
    │   │   └── index.ts
    │   └── ui.tsx                    # DELETED (the legacy shim) — 70 importers moved
    ├── team/
    │   ├── materials/
    │   │   ├── actions.ts            # NEW: the registry (D9)
    │   │   ├── useMaterialActionList.ts  # NEW: registry × material × context → list
    │   │   ├── MaterialActionMenu.tsx    # NEW: the one grouped menu
    │   │   ├── MaterialInlineActions.tsx # NEW: the first N, never more than four
    │   │   ├── MaterialDetail.tsx        # NEW: the one detail surface (US5)
    │   │   ├── MaterialActionHost.tsx    # NEW: mounts the dialogs an action opens
    │   │   └── tail.ts               # unchanged
    │   ├── processing/
    │   │   └── CompressionProvider.tsx   # NEW: the queue lifted out of ExplorerShell
    │   ├── palette/
    │   │   ├── WorkspacePalette.tsx  # NEW (⌘K)
    │   │   └── ShortcutSheet.tsx     # NEW
    │   ├── explorer/                 # ExplorerShell split; rows/tiles/search render the surface
    │   ├── tasks/                    # autosave, two-column layout, attachments first-class
    │   ├── accounts/                 # onto Table; hand-rolled menu deleted
    │   ├── workspace/                # chrome fixes: chips, sections, addressable dialogs
    │   ├── members/                  # one screen, not two
    │   └── …
    └── dev/DesignSystemPage.tsx      # gains the new patterns

scripts/
├── check-tailwind-classes.mjs        # NEW gate (D6)
├── check-design-tokens.mjs           # unchanged
├── verify-styles.mjs                 # unchanged
└── verify-all.mjs                    # + the new gate

tests/
├── material-action-registry.test.ts      # NEW — SC-003, SC-004
├── material-action-surfaces.test.tsx     # NEW — identical across five hosts
├── team-raw-controls.test.ts             # NEW — SC-002, the count is zero
├── theme-bridge.test.ts                  # NEW — every HeroUI variable defined, both themes
├── task-editor-autosave.test.tsx         # NEW — SC-005
├── design-components.test.tsx            # rewritten onto the inventory's markers
├── ui-consistency.test.tsx               # unchanged
└── performance-budgets.test.ts           # unchanged; baseline re-measured once

.specify/memory/constitution.md           # VI amended, 2.0.0 → 3.0.0
docs/DESIGN.md, CLAUDE.md, AGENTS.md      # restate the amended rule
THIRD_PARTY_NOTICES.md                    # the new tree
performance-baseline.json                 # re-ratcheted once, with the reason
```

**Structure Decision**: `apps/web` only, in the directories above. The team workspace keeps its
existing feature-folder layout; three new folders appear (`team/materials/` gains components,
`team/palette/`, `team/processing/` gains the provider) because each holds a responsibility the
audit found smeared across `ExplorerShell.tsx`'s 2,231 lines. Nothing is added outside `apps/web`
except the four gate/doc files at the repository root, so the web-only deploy gate stays
satisfied.

## The flows pass (US10–US14)

Added after the first beta walk. Everything below stays web-only: every piece of data it needs
already reaches the browser. The two facts that make that true were checked in code, not
assumed — a finished agent run reports the id of the material it wrote
(`useAgentQueue.ts`, `finished.materialId`; `MaterialProcessFlow.tsx`, `outcome.materialId`), and
`attachTaskMaterials` already answers `attached / alreadyAttached / rejected`.

### F1 — The agent queue belongs to the space (FR-077, FR-078)

`useAgentQueue` is called inside `ExplorerShell`, so a run exists only while Files is mounted and
only Files can start one. It moves into `team/processing/AgentQueueProvider.tsx`, mounted by
`WorkspaceShell` beside `LibraryProcessingProvider`; `ExplorerShell` reads it from context. This is
T043, finally with its reason.

A queue item gains `attachTo?: { taskId: string }`. When an item finishes with a material id and
carries `attachTo`, the provider calls `attachTaskMaterials`; `alreadyAttached` is silent, a
missing task is silent, and success raises one toast — "Added to the task" with **Take off**, which
detaches. The single-material process dialog takes an `onFinished(materialId)` callback for the same
purpose.

### F2 — Every "Make" action on a task attachment (FR-076, FR-081)

`TaskAttachmentTile` wires the handlers it lacks: `transcribe` (queue, with `attachTo`),
`process` (`MaterialProcessFlow` over the task, `onFinished` attaches), `copyText` and `editText`
for transcripts. The registry's standalone `compress` has no handler anywhere and duplicates
`process`'s compressor tool; it is deleted, and `process` becomes the one entry (FR-041).

Inline on a tile: **2** actions plus "…" (open and the kind's primary make — catalog for a video),
not 4 — the tile is a card in a grid, and five icons on a card is the chaos this pass removes.

### F3 — Settings over the surface that needs them (FR-079)

The editor's two settings links (re-stitch, catalog) stop calling `onClose()` + `navigateTo` into
Files. They push the current address with `settings=1&tab=…`; since T106 the dialog rides over any
section, the `task` parameter stays, and the editor stays mounted under it.

### F4 — The way back from "Show in folder" (FR-080)

The reveal writes `back=<taskId>` into the Files address. Files' toolbar shows a return chip
("← To the task") while `back` is present; pressing it goes to `/tasks?task=<id>`. Any other Files
navigation drops `back`.

### F5 — Download that the browser cannot take (FR-082)

The tile's own download (a copy of the grant logic) is replaced by the shared download handler
the explorer host already uses, which maps the grant's refusal to words and offers the local app.

### F6 — Add to task (FR-083, FR-084)

A registry action `addToTask` (group **place**) and a `team/tasks/AddToTaskDialog.tsx`: a search
field over the space's tasks (the board's own `listTasks`), most recently changed first, Enter to
add. Wired in Files rows and tiles, the detail pane, search results and the selection bar. It never
navigates; the toast names the task and opens it.

### F7 — The editor as a brief (FR-085 – FR-088)

- `.team-task-editor` becomes an inline-size container; the two-column grid is a **container
  query** at 820 px, so the dialog's width decides, not the window's. The dialog grows to `xl`.
- Left column: title, one facts row (status · assignee · date), the brief (`field-sizing:
content`, min 3 rows), one compact progress row (slider + maximum). Right column: materials first,
  then accounts, then tags.
- `TaskStatusControl` renders its segmented form and a `Select`; a container query shows one.
  No label wraps (`white-space: nowrap`), which is also what makes the switch necessary.
- The heading's generic "Task details" gives way to the save state and an editor menu ("…") that
  holds Delete. Delete closes the editor and raises a toast with Undo, which re-creates the task
  with its fields and attachments; the board's bulk delete already follows that model.
- The attachments hint paragraph becomes the drop zone's own text.

### F8 — Solo (FR-089, FR-090)

`solo = members.length <= 1`, from the member list the editor and the board already load, and
updated by the realtime member refresh. Assignee select, invite button and the assignee filter
render nothing while solo. The board heading gains a quick-add field; its Enter creates a task with
that title and does not open the editor.

### F9 — Calm at rest (FR-091 – FR-097)

- Files: `has-pane` only with a selection.
- Board and Accounts: the heading's primary action is hidden while the empty state carries it;
  Accounts hides its toolbar with zero accounts.
- Rows and tiles lose `ShareButton` (the "…" has share and copy link); the "opens in Google Drive"
  line becomes an icon with a tooltip; folder tiles drop the "Folder" caption and shrink.
- Header: the eyebrow goes; Trash, the updater's idle link, Settings and Shortcuts move into one
  "Space" `DropdownMenu`; a palette trigger (search icon, `⌘K` in its tooltip) sits beside it; the
  storage chip renders only for non-healthy states, the updater chip only while running.
- Members: one heading; a member row keeps Edit and moves Remove and Transfer into "…".
- Detail pane: `transcribe` is not wired in `PaneActions` while `VideoTextActions` is shown.
- A test (`tests/workspace-at-rest.test.tsx`) renders the empty board, empty accounts, Files at
  rest and the header, and asserts one primary and no duplicated accessible name per surface.

The accounts row and the updater row densities listed in the spec's table are carried by the
already-planned T116–T131, which gain the concrete targets.

## Complexity Tracking

| Violation                                                                                                                                       | Why needed                                                                                                                                                                                                                                      | Simpler alternative rejected because                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Principle VI: a styling library and a utility layer replace "one global stylesheet"                                                             | The owner chose HeroUI to get a complete, accessible, beautiful component set instead of maintaining 45 bespoke controls and five hand-rolled roving-focus implementations. The library cannot be themed or composed without its utility layer. | Keeping the bespoke system: the audit's numbers (104 raw buttons, two calendars, two menu implementations, two dialog implementations, two `Button` generations) are what "keep maintaining it" has produced in one cycle. Vendoring only HeroUI's CSS: its components are tailwind-variants functions, so the CSS without the components is not the library. |
| A second component generation exists during the migration (inventory adapters over HeroUI, while un-migrated screens still use the old classes) | Layer order (D4) makes each screen's migration a deletion. A flag day across 128 modules is not reviewable.                                                                                                                                     | A single atomic rewrite: the previous migration's finding S1 is that the phased floor inverted the cost in the project's favour; repeating it is the cheaper path, not the more complex one.                                                                                                                                                                  |
| The compression queue is lifted into a provider                                                                                                 | A task must be able to compress an attachment without the explorer being mounted (FR-012, FR-019).                                                                                                                                              | Leaving it in `ExplorerShell`: it is the single reason a whole class of actions cannot be offered anywhere else, and the same lift was already made for batches by `LibraryProcessingProvider`.                                                                                                                                                               |
