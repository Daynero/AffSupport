{
  "note": "Keys assembled or returned at runtime rather than written as literals at a call site, so the unused-key scan must not delete them. Kept short deliberately: an exception list that grows on its own stops meaning anything. 011 adds the five thumbnail reasons, assembled from `provider_thumbnail_reason` at render.",
  "keys": [
    "selectedOne",
    "selectedFew",
    "selectedMany",
    "teamExplorerThumbnail_corrupt",
    "teamExplorerThumbnail_protected",
    "teamExplorerThumbnail_provider_missing",
    "teamExplorerThumbnail_too_large",
    "teamExplorerThumbnail_unsupported"
  ]
}
